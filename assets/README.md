Place the new BrisConnect logo image here as `logo.png` or `logo.svg`.

Recommended:
- File name: `logo.png` or `logo.svg`
- Size: 512×512 PNG (or square SVG)

After adding the file, refresh `web/index.html` in your browser (or serve the site) to see the updated logo.
